#include<bits/stdc++.h>
#include<string.h>
#define MAX 100
using namespace std;
struct GiftCarpet
{
    string str;
};

int main(){
bool ans;
int t;
cin>>t;
for (int i = 0; i < t; i++)
    {
        int n, size;
        cin>>n>>size;
        for (int j = 0; j < n; j++)
        {
            GiftCarpet strs[MAX];
            getline(cin, strs[i].str);   
            
        }
    }
}